```
$ git clone https://github.com/mk2e/mk2e-server.git
$ cd mk2e-server

$ docker-compose up -d

컨테이널 터미널 환경에 접속하기
$ docker exec -i -t mysql /bin/bash

위 터미널 환경에 접속 시 mysql db 진입 방법
$ mysql -u root -p
Password 입력: root
```
